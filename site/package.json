{
  "name": "cif-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "prebuild": "npm run check:record",
    "build": "astro build",
    "check:record": "node scripts/validate-record-corpus.mjs",
    "preview": "astro preview",
    "test": "npm run test:functional",
    "test:functional": "npm run build && playwright test"
  },
  "dependencies": {
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0"
  }
}
